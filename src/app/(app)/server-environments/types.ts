// Shared types for the Server Environments feature.
// Re-exported from the data layer so this folder has a single, local
// place to import types from, without duplicating the definition.
export type { ServerEnvironment } from "../../../lib/supabase";
