import type { ServerType } from '../../../lib/supabase';

// Re-exported so every file in this feature folder can import ServerType
// from a single local location instead of reaching back into lib/supabase.
export type { ServerType };
